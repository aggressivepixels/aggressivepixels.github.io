{
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "postinstall": "spago install",
    "serve": "spago run --main Serve"
  },
  "dependencies": {
    "purescript": "^0.13.6",
    "purty": "^6.0.2",
    "spago": "^0.14.0"
  }
}
