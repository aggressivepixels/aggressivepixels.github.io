{
  "scripts": {
    "build": "spago run",
    "run-and-watch": "spago run --watch",
    "test": "spago test",
    "postinstall": "spago install",
    "serve": "spago run --main Serve"
  },
  "dependencies": {
    "purescript": "^0.13.8",
    "purty": "^6.2.0",
    "spago": "^0.15.2"
  }
}
